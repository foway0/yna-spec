sites:
  get:
    x-eov-operation-id: list
    x-eov-operation-handler: sites
    tags:
      - site
    description: site list
    responses:
      200:
        description: OK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SiteList'