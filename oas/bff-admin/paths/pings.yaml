ping:
  get:
    x-eov-operation-id: ping
    x-eov-operation-handler: ping
    tags:
      - system
    description: |
      ping then pong
    responses:
      200:
        description: OK
        content:
          text/plain:
            schema:
              type: string
              example: pong