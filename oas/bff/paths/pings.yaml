ping:
  get:
    tags:
      - system
    description: |
      ping then pong
    responses:
      200:
        description: OK
        content:
          text/plain:
            schema:
              type: string
              example: pong